# 🔄 Автообновление WOXLY Desktop

## 📋 Как это работает

WOXLY Desktop использует **electron-updater** для автоматического обновления приложения без участия пользователя.

### Схема работы:

```
1. Приложение запускается
   ↓
2. Через 10 секунд проверяет обновления на сервере
   ↓
3. Если найдена новая версия → загружает её в фоне
   ↓
4. Показывает уведомление "Обновление готово"
   ↓
5. При следующем запуске → автоматически устанавливает
```

---

## ⚙️ Настройка сервера для обновлений

### Шаг 1: Создайте папку на сервере

```bash
ssh root@woxly.ru
mkdir -p /var/www/woxly.ru/downloads/desktop
cd /var/www/woxly.ru/downloads/desktop
```

### Шаг 2: Структура файлов

После сборки вам нужно загрузить на сервер:

```
/var/www/woxly.ru/downloads/desktop/
├── latest.yml                          # Информация о последней версии
├── Woxly-Setup-1.0.0.exe              # Установщик
└── Woxly-Setup-1.0.0.exe.blockmap     # Карта блоков для дельта-обновлений
```

### Шаг 3: Настройте Nginx

Добавьте в конфигурацию Nginx:

```nginx
server {
    listen 443 ssl http2;
    server_name woxly.ru;

    # ... существующие настройки ...

    # Папка с обновлениями
    location /downloads/desktop {
        alias /var/www/woxly.ru/downloads/desktop;
        autoindex off;
        
        # CORS для автообновлений
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods "GET, HEAD, OPTIONS";
        
        # Кеширование
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }
}
```

Перезапустите Nginx:
```bash
nginx -t
systemctl reload nginx
```

---

## 📦 Процесс выпуска обновления

### 1. Увеличьте версию

Отредактируйте `C:\woxly\apps\desktop\package.json`:

```json
{
  "version": "1.0.1"  // Было: 1.0.0
}
```

### 2. Соберите приложение

```powershell
cd C:\woxly\apps\desktop
npm run package
```

### 3. Найдите файлы

После сборки файлы будут в:
```
C:\woxly\apps\desktop\release\
├── latest.yml
├── Woxly-Setup-1.0.1.exe
└── Woxly-Setup-1.0.1.exe.blockmap
```

### 4. Загрузите на сервер

```powershell
# Используйте SCP или FTP
scp release/latest.yml root@woxly.ru:/var/www/woxly.ru/downloads/desktop/
scp release/Woxly-Setup-1.0.1.exe root@woxly.ru:/var/www/woxly.ru/downloads/desktop/
scp release/Woxly-Setup-1.0.1.exe.blockmap root@woxly.ru:/var/www/woxly.ru/downloads/desktop/
```

### 5. Готово!

Все пользователи получат обновление автоматически в течение 4 часов.

---

## 🎯 Как работает автообновление

### Автоматическая проверка

```typescript
// Проверка каждые 4 часа
setInterval(() => {
  autoUpdater.checkForUpdates();
}, 4 * 60 * 60 * 1000);

// Первая проверка через 10 секунд после запуска
setTimeout(() => {
  autoUpdater.checkForUpdates();
}, 10000);
```

### События обновления

| Событие | Описание | Действие |
|---------|----------|----------|
| `update-available` | Найдена новая версия | Показывает уведомление "Загружается..." |
| `download-progress` | Прогресс загрузки | Отправляет процент в UI |
| `update-downloaded` | Обновление загружено | Показывает "Готово к установке" |
| `update-not-available` | Обновлений нет | Логирует в консоль |
| `error` | Ошибка обновления | Логирует ошибку |

### Настройки

```typescript
autoUpdater.autoDownload = true;           // Автоматически загружать
autoUpdater.autoInstallOnAppQuit = true;   // Установить при выходе
```

---

## 🔔 Уведомления для пользователя

### 1. Обновление найдено
```
🎉 Доступно обновление
Новая версия 1.0.1 загружается...
```

### 2. Прогресс загрузки
```
⬇️ Загружено 45%
```

### 3. Готово к установке
```
✅ Обновление готово
Версия 1.0.1 готова к установке. Нажмите для перезапуска.
```

---

## 🛠️ Ручная проверка обновлений

Пользователи могут проверить обновления вручную через настройки:

```typescript
// В настройках приложения
const checkUpdates = async () => {
  const result = await window.electron.checkForUpdates();
  if (result.checking) {
    console.log('Проверка обновлений...');
  }
};
```

---

## 📊 Типы обновлений

### Дельта-обновления (рекомендуется)

Загружаются только изменённые части:
- ✅ Быстрее (10-50 MB вместо 150 MB)
- ✅ Экономия трафика
- ✅ Автоматически если есть `.blockmap` файл

### Полное обновление

Загружается весь установщик:
- ⚠️ Медленнее (150-200 MB)
- ⚠️ Больше трафика
- ✅ Надёжнее для больших изменений

---

## 🔒 Безопасность

### Проверка подписи (опционально)

Для максимальной безопасности подпишите обновления:

```json
// package.json
{
  "build": {
    "win": {
      "certificateFile": "path/to/cert.pfx",
      "certificatePassword": "your-password",
      "signingHashAlgorithms": ["sha256"],
      "verifyUpdateCodeSignature": true
    }
  }
}
```

### HTTPS обязателен

Обновления работают **только через HTTPS**:
```
✅ https://woxly.ru/downloads/desktop/latest.yml
❌ http://woxly.ru/downloads/desktop/latest.yml
```

---

## 🐛 Отладка обновлений

### Включите логи

```typescript
// main.ts
autoUpdater.logger = require('electron-log');
autoUpdater.logger.transports.file.level = 'info';
```

### Проверьте файл latest.yml

```yaml
version: 1.0.1
files:
  - url: Woxly-Setup-1.0.1.exe
    sha512: [hash]
    size: 156789123
path: Woxly-Setup-1.0.1.exe
sha512: [hash]
releaseDate: '2024-12-13T10:00:00.000Z'
```

### Тестирование

```powershell
# 1. Соберите версию 1.0.0
npm run package

# 2. Установите её
.\release\Woxly-Setup-1.0.0.exe

# 3. Измените версию на 1.0.1
# 4. Соберите снова
npm run package

# 5. Загрузите на сервер
# 6. Запустите приложение версии 1.0.0
# 7. Оно должно обновиться до 1.0.1
```

---

## 📝 Changelog

Создайте файл `CHANGELOG.md` для пользователей:

```markdown
# Changelog

## [1.0.1] - 2024-12-13
### Добавлено
- Улучшенная обрезка аватарок
- Плавные анимации
- Автообновление

### Исправлено
- Баг с WebSocket соединением
- Проблемы с аватарами

## [1.0.0] - 2024-12-10
- Первый релиз
```

---

## 🎉 Готово!

Теперь ваше приложение:
- ✅ Автоматически проверяет обновления каждые 4 часа
- ✅ Загружает их в фоне
- ✅ Уведомляет пользователя
- ✅ Устанавливает при следующем запуске
- ✅ Поддерживает дельта-обновления

---

## 📚 Дополнительная информация

### Документация electron-updater:
https://www.electron.build/auto-update

### Формат latest.yml:
https://www.electron.build/auto-update#file-generated-and-uploaded-in-addition

### Примеры:
- Discord Desktop
- Slack Desktop
- VS Code
- Atom

Все они используют electron-updater! 🚀






