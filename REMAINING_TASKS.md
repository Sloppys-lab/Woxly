# Оставшиеся задачи WOXLY (4 из 19)

## 🔴 Задачи, требующие backend разработки

### 1. Синхронизация названия/фото комнаты

**Требования**:
- **Личные (DM)**: Если пользователь1 меняет название/фото, видит только он
- **Групповые (3+)**: Если кто-то меняет, видят все сразу

**Что нужно сделать**:

#### Backend:
1. Добавить таблицу `RoomCustomization` в Prisma:
```prisma
model RoomCustomization {
  id        Int      @id @default(autoincrement())
  userId    Int
  roomId    Int
  customName String?
  customAvatar String?
  user      User     @relation(fields: [userId], references: [id])
  room      Room     @relation(fields: [roomId], references: [id])
  @@unique([userId, roomId])
}
```

2. API endpoints:
- `PUT /rooms/:id/customize` - сохранить персональные настройки
- `GET /rooms/:id/customization` - получить персональные настройки

3. Socket.IO событие для групповых комнат:
- `room-updated` - отправлять всем участникам при изменении

#### Frontend:
1. Обновить `RoomSettingsModal` для проверки типа комнаты
2. Сохранять в БД вместо localStorage для DM
3. Слушать `room-updated` для групповых комнат

**Оценка времени**: 2-3 часа

---

### 2. Мгновенное обновление аватара/ника

**Требования**:
- Когда пользователь меняет аватар/ник
- Обновляется у всех друзей сразу
- Без перезагрузки страницы

**Что нужно сделать**:

#### Backend:
1. Добавить Socket.IO событие в `/users/me` и `/users/me/avatar`:
```typescript
// После обновления пользователя
const friends = await prisma.friendship.findMany({
  where: {
    OR: [
      { userId: req.user!.id, status: 'accepted' },
      { friendId: req.user!.id, status: 'accepted' }
    ]
  }
});

// Отправляем событие всем друзьям
friends.forEach(friendship => {
  const friendId = friendship.userId === req.user!.id 
    ? friendship.friendId 
    : friendship.userId;
  io.to(`user-${friendId}`).emit('friend-updated', {
    userId: req.user!.id,
    username: updatedUser.username,
    avatarUrl: updatedUser.avatarUrl
  });
});
```

#### Frontend:
1. Слушать событие `friend-updated` в `useFriendsStore`
2. Обновлять друга в списке
3. Обновлять в активной комнате, если открыта

**Оценка времени**: 1 час

---

### 3. Система звонков (БОЛЬШАЯ ЗАДАЧА!)

**Требования**:
1. Пользователь1 звонит → автоматически попадает в звонок
2. У пользователя2 иконка пульсирует
3. Пользователь2 принимает → попадает в звонок
4. Пользователь2 не принимает → иконка исчезает через 15 сек
5. Если пользователь1 один 15 минут → автоматически выходит
6. Если пропустил звонок → кнопка "Присоединиться"
7. Иконка пульсирует ТОЛЬКО у того, кому звонят

**Что нужно сделать**:

#### Backend:
1. Добавить таблицу `CallSession`:
```prisma
model CallSession {
  id          Int      @id @default(autoincrement())
  roomId      Int
  callerId    Int
  startedAt   DateTime @default(now())
  endedAt     DateTime?
  participants Json     // { userId: { status: 'ringing' | 'connected' | 'left', joinedAt: DateTime } }
  room        Room     @relation(fields: [roomId], references: [id])
  caller      User     @relation(fields: [callerId], references: [id])
}
```

2. API endpoints:
- `POST /calls/start` - начать звонок
- `POST /calls/:id/join` - присоединиться
- `POST /calls/:id/leave` - выйти
- `GET /calls/active/:roomId` - получить активный звонок

3. Socket.IO события:
- `call-started` - звонок начат
- `call-participant-joined` - участник присоединился
- `call-participant-left` - участник вышел
- `call-ended` - звонок завершен

4. Таймеры:
- 15 секунд на ответ (после - иконка исчезает)
- 15 минут одиночества (после - автовыход)

#### Frontend:
1. Обновить `CallModal`:
- Автоматический переход при звонке
- Пульсация иконки (animate-pulse-soft)
- Таймеры
- Кнопка "Присоединиться"

2. Обновить `MainContent`:
- Показывать кнопку "Присоединиться" если есть активный звонок

3. Обновить `VoiceParticipantCard`:
- Пульсация только для ringing статуса

**Оценка времени**: 4-5 часов

---

### 4. Добавление друзей в звонок

**Требования**:
1. Кнопка "Добавить друга" в звонке
2. Список друзей (кроме тех, кто в звонке)
3. Выбор нескольких друзей
4. Создание новой групповой комнаты
5. Перенос звонка в новую комнату
6. Дозвон всем приглашенным

**Что нужно сделать**:

#### Backend:
1. API endpoint:
```typescript
POST /calls/:id/add-participants
Body: { friendIds: number[] }

// Логика:
// 1. Создать новую групповую комнату
// 2. Добавить всех участников
// 3. Перенести звонок
// 4. Отправить incoming-call всем новым участникам
```

2. Socket.IO:
- `call-transferred` - звонок перенесен в новую комнату
- Отправить `incoming-call` новым участникам

#### Frontend:
1. Обновить `CallModal`:
- Мультивыбор друзей (чекбоксы)
- Кнопка "Пригласить"
- Обработка переноса звонка

**Оценка времени**: 2-3 часа

---

### 5. Регистрация с email верификацией

**Требования**:
1. Убрать userTag
2. Email верификация с кодом
3. Усложнить пароль (строчные, заглавные, цифры, символы)
4. Валидация никнейма (a-z, а-я, 0-9, . _, 5-13 символов)

**Что нужно сделать**:

#### Backend:
1. Установить Nodemailer:
```bash
npm install nodemailer
npm install --save-dev @types/nodemailer
```

2. Настроить SMTP (например, Gmail):
```typescript
import nodemailer from 'nodemailer';

const transporter = nodemailer.createTransport({
  service: 'gmail',
  auth: {
    user: process.env.EMAIL_USER,
    pass: process.env.EMAIL_PASSWORD
  }
});
```

3. Добавить таблицу `EmailVerification`:
```prisma
model EmailVerification {
  id        Int      @id @default(autoincrement())
  email     String
  code      String
  expiresAt DateTime
  createdAt DateTime @default(now())
}
```

4. API endpoints:
- `POST /auth/register/send-code` - отправить код
- `POST /auth/register/verify-code` - проверить код
- `POST /auth/register/complete` - завершить регистрацию

5. Валидация:
- Никнейм: `/^[a-zA-Zа-яА-Я0-9._]{5,13}$/`
- Пароль: `/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/`

#### Frontend:
1. Обновить `RegisterPage`:
- Убрать userTag
- Добавить валидацию пароля
- Добавить шаг с вводом кода
- Показывать требования к паролю

**Оценка времени**: 3-4 часа

---

## 📈 Общая оценка

**Общее время для оставшихся задач**: ~12-16 часов

**Приоритет**:
1. ⭐⭐⭐ Мгновенное обновление аватара/ника (быстро, полезно)
2. ⭐⭐ Синхронизация комнат (средняя сложность)
3. ⭐ Добавление друзей в звонок (можно отложить)
4. ⭐ Система звонков (очень сложно, можно отложить)
5. ⭐ Регистрация с email (можно отложить)

---

## 🎯 Рекомендация

**Сейчас**:
1. Протестировать текущие 15 изменений
2. Убедиться, что всё работает
3. Собрать отзыв

**Потом**:
4. Сделать backend для оставшихся 4 задач
5. Это отдельная большая сессия разработки

---

## ✅ Текущий результат

**79% задач выполнено!**

Приложение уже значительно улучшено:
- Красивый дизайн с красными акцентами
- Удобные функции (переименование, закрепление)
- Плавные анимации
- Адаптивная верстка
- Красивый установщик

**Готово к тестированию!** 🚀
