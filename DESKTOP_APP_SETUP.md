# 🪟 Инструкция по созданию Windows приложения Woxly

## 📋 Что было создано

Полноценное Electron приложение для Windows с:
- ✅ Системным треем
- ✅ Нативными уведомлениями
- ✅ Автозапуском
- ✅ Автообновлениями
- ✅ Установщиком (.exe)

## 🚀 Шаг 1: Установка зависимостей

```bash
cd c:\woxly\apps\desktop
npm install
```

## 🎨 Шаг 2: Создание иконок

### Вариант А: Использовать готовую иконку

Создайте папку `build` и добавьте:
- `icon.ico` (256x256) - для Windows
- `icon.png` (512x512) - для трея

### Вариант Б: Конвертировать из PNG

```bash
# Установите ImageMagick
# Затем конвертируйте:
magick convert icon.png -define icon:auto-resize=256,128,64,48,32,16 icon.ico
```

### Вариант В: Онлайн конвертер

1. Откройте https://convertio.co/ru/png-ico/
2. Загрузите PNG (512x512)
3. Скачайте ICO
4. Поместите в `apps/desktop/build/`

## 🔧 Шаг 3: Настройка

### Изменить URL сервера (опционально)

Откройте `apps/desktop/src/main.ts`:

```typescript
const SERVER_URL = 'https://woxly.ru'; // Ваш URL
```

### Настроить автообновления (опционально)

В `apps/desktop/package.json` добавьте:

```json
"publish": {
  "provider": "github",
  "owner": "your-username",
  "repo": "woxly"
}
```

## 🏗️ Шаг 4: Разработка

### Запуск в режиме разработки

```bash
cd apps/desktop
npm run dev
```

Откроется окно приложения с DevTools.

### Тестирование функций

1. **Системный трей**: Закройте окно - оно свернется в трей
2. **Уведомления**: Проверьте Windows уведомления
3. **Автозапуск**: Включите в настройках

## 📦 Шаг 5: Сборка

### Вариант А: Полная сборка (Рекомендуется)

```bash
npm run dist
```

Создаст в `release/`:
- `Woxly-Setup-1.0.0.exe` - Установщик (~150 MB)
- `Woxly-Portable-1.0.0.exe` - Портативная версия (~150 MB)

### Вариант Б: Только установщик

```bash
npm run package
```

### Вариант В: Без упаковки (для тестирования)

```bash
npm run package:dir
```

Создаст папку `release/win-unpacked/` с приложением.

## 🎯 Шаг 6: Тестирование установщика

1. Найдите `Woxly-Setup-1.0.0.exe` в папке `release/`
2. Запустите установщик
3. Выберите папку установки
4. Дождитесь завершения
5. Запустите Woxly из меню Пуск

## 🔄 Шаг 7: Обновление версии

### Изменить версию

В `apps/desktop/package.json`:

```json
{
  "version": "1.0.1"
}
```

### Пересобрать

```bash
npm run dist
```

## 🌐 Шаг 8: Публикация (опционально)

### GitHub Releases

1. Создайте релиз на GitHub
2. Загрузите файлы из `release/`
3. Пользователи получат автообновление

### Microsoft Store

1. Зарегистрируйтесь в Partner Center
2. Создайте приложение
3. Загрузите установщик
4. Пройдите сертификацию

## 🎨 Кастомизация

### Изменить название

В `package.json`:

```json
{
  "name": "@woxly/desktop",
  "productName": "Woxly",
  "description": "Ваше описание"
}
```

### Изменить размер окна

В `src/main.ts`:

```typescript
mainWindow = new BrowserWindow({
  width: 1400,
  height: 900,
  minWidth: 1000,
  minHeight: 700,
});
```

### Добавить горячие клавиши

В `src/main.ts`:

```typescript
import { globalShortcut } from 'electron';

app.whenReady().then(() => {
  // Ctrl+Shift+W - показать окно
  globalShortcut.register('CommandOrControl+Shift+W', () => {
    mainWindow?.show();
  });
});
```

## 🐛 Решение проблем

### Ошибка "icon.ico not found"

Создайте папку `build/` и добавьте `icon.ico`.

### Ошибка при сборке

```bash
# Очистите кэш
rm -rf node_modules dist dist-vite release
npm install
npm run dist
```

### Приложение не запускается

1. Проверьте логи в `%APPDATA%\Woxly\logs\`
2. Откройте DevTools в режиме разработки
3. Проверьте URL сервера

### Большой размер установщика

Это нормально для Electron (~150 MB). Включает:
- Chromium (~100 MB)
- Node.js (~30 MB)
- Ваше приложение (~20 MB)

## 📊 Сравнение вариантов

| Вариант | Размер | Установка | Обновления | Сложность |
|---------|--------|-----------|------------|-----------|
| Electron | ~150 MB | Да | Авто | Средняя |
| PWA | ~5 MB | Нет | Авто | Легкая |
| Tauri | ~10 MB | Да | Авто | Высокая |

## ✅ Checklist перед релизом

- [ ] Иконки созданы (icon.ico, icon.png)
- [ ] URL сервера настроен
- [ ] Версия обновлена
- [ ] Протестирован в режиме разработки
- [ ] Собран установщик
- [ ] Протестирована установка
- [ ] Протестированы все функции:
  - [ ] Системный трей
  - [ ] Уведомления
  - [ ] Автозапуск
  - [ ] Звонки
  - [ ] Сообщения
- [ ] LICENSE.txt заполнен

## 🎉 Готово!

Теперь у вас есть полноценное Windows приложение Woxly!

### Следующие шаги:

1. **Тестирование** - протестируйте на разных версиях Windows
2. **Публикация** - опубликуйте на GitHub или в Microsoft Store
3. **Маркетинг** - расскажите пользователям о десктопной версии
4. **Обновления** - регулярно выпускайте обновления

## 📞 Поддержка

Если нужна помощь:
1. Проверьте README.md в apps/desktop/
2. Откройте issue на GitHub
3. Напишите в поддержку









