# 🚀 Desktop - Руководство по разработке и запуску

## 📋 Режимы работы

### 1. **DEV режим** (для разработки)
Приложение загружает UI с Vite dev сервера `http://localhost:3000` - все изменения применяются мгновенно (hot reload).

```bash
cd c:\woxly\apps\desktop
npm run dev
```

**Что происходит:**
- Запускается Vite dev server на `http://localhost:3000`
- Запускается Electron и загружает `http://localhost:3000`
- Hot Reload - изменения в коде сразу видны
- DevTools открыт автоматически

---

### 2. **Production режим** (готовое приложение)
Приложение работает с локальными файлами (как Discord), подключается только к API.

#### Шаг 1: Собрать frontend для desktop
```bash
cd c:\woxly\apps\frontend
npm run build:desktop
```

Это создаст папку `c:\woxly\apps\desktop\dist-vite` с готовыми HTML/JS/CSS файлами.

#### Шаг 2: Собрать Electron
```bash
cd c:\woxly\apps\desktop
npm run build:electron
```

#### Шаг 3: Запустить
```bash
cd c:\woxly\apps\desktop
npm start
```

**Что происходит:**
- Electron загружает локальный `dist-vite/index.html`
- React UI работает локально
- API запросы идут на `https://woxly.ru/api`
- База данных на сервере

---

### 3. **Создать .exe установщик**

```bash
cd c:\woxly\apps\desktop
npm run package
```

Это создаст:
- `release/Woxly-Setup-1.0.0.exe` - установщик (NSIS)
- `release/Woxly-Portable-1.0.0.exe` - portable версия

**Требования:**
- Иконка `.ico` файл → `apps/desktop/build/icon.ico`
- Лицензия → `apps/desktop/LICENSE.txt`

---

## 🔍 Отладка черного экрана

### Проблема: Черный экран при запуске

#### 1. Проверьте консоль в DevTools
DevTools открывается автоматически. Ищите ошибки:

```javascript
// Хорошие логи (все работает):
[APP] Current user: null
[APP] Current path: /auth/login
[APP] Has access token: false

// Плохие логи (проблема):
Failed to fetch
Network Error
404 Not Found
```

#### 2. Проверьте что файлы собраны

```powershell
# Должен существовать:
Test-Path "c:\woxly\apps\desktop\dist-vite\index.html"
# Вывод: True

# Должны существовать JS файлы:
Get-ChildItem "c:\woxly\apps\desktop\dist-vite\assets" -Filter "*.js"
```

#### 3. Проверьте API URL

Откройте `dist-vite/assets/main-*.js` и найдите:
```javascript
"https://woxly.ru/api"  // ← Должен быть этот URL
```

#### 4. Попробуйте DEV режим

```bash
cd c:\woxly\apps\desktop
npm run dev
```

Если в DEV режиме работает, но в production черный экран - проблема со сборкой.

---

## 🎨 Почему может быть черный экран?

### Причина 1: Не авторизованы
**Решение**: Это нормально! Просто войдите в аккаунт.

На странице логина (`/auth/login`) должны быть видны:
- Логотип "WOXLY" (белый текст)
- Поля "Email" и "Пароль"
- Кнопка "Войти"

Если их НЕ видно - проблема с CSS.

### Причина 2: CSS не загрузился
Проверьте в DevTools → Network:
```
main-*.css    200 OK ✅
```

Если `404 Not Found` ❌ - пересоберите:
```bash
cd c:\woxly\apps\frontend
npm run build:desktop
```

### Причина 3: Ошибка JavaScript
Откройте Console в DevTools и посмотрите на ошибки.

Частые ошибки:
- `Failed to fetch` - нет интернета или сервер недоступен
- `localStorage is not defined` - проблема с Electron preload
- `Cannot read property of undefined` - ошибка в коде React

---

## ✅ Проверка что все работает

### 1. Файлы собраны
```powershell
dir c:\woxly\apps\desktop\dist-vite
# Должны быть:
# - index.html
# - assets/*.js
# - assets/*.css
```

### 2. Electron собран
```powershell
dir c:\woxly\apps\desktop\dist
# Должны быть:
# - main.js
# - preload.js
```

### 3. Приложение запускается
```powershell
cd c:\woxly\apps\desktop
npm start
# Должно открыться окно с белым логотипом "WOXLY"
```

### 4. API доступен
Откройте консоль DevTools и выполните:
```javascript
fetch('https://woxly.ru/api/health').then(r => r.json()).then(console.log)
// Ожидается: {status: "ok"} или подобное
```

---

## 🐛 Типичные проблемы и решения

### Проблема: "Module not found: @woxly/ui"
```bash
cd c:\woxly\packages\ui
npm install

cd c:\woxly\apps\frontend
npm install
```

### Проблема: "terser not found"
**Решение**: Уже исправлено, используется `esbuild` вместо `terser`.

### Проблема: Electron кэш ошибки
```
ERROR:cache_util_win.cc(20)] Unable to move the cache
```

**Решение**: Это не критично, игнорируйте.

Если хотите исправить:
```powershell
Remove-Item -Recurse -Force "$env:APPDATA\woxly-desktop"
```

### Проблема: Service Worker ошибки
**Решение**: Service Worker не используется в Desktop, можно отключить:

В `index.html` удалите:
```javascript
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('/sw.js')
}
```

---

## 📁 Структура файлов

```
apps/desktop/
├── src/
│   ├── main.ts              # Главный процесс Electron
│   └── preload.ts           # Preload скрипт
├── dist/                    # Скомпилированный Electron (TypeScript → JS)
│   ├── main.js
│   └── preload.js
├── dist-vite/               # Собранный frontend (из apps/frontend)
│   ├── index.html
│   └── assets/
│       ├── main-*.js        # React приложение
│       ├── main-*.css       # Стили
│       └── react-vendor-*.js
├── build/
│   ├── icon.ico             # Иконка для .exe
│   └── icon.png             # Иконка для трея
├── package.json
└── tsconfig.electron.json
```

---

## 🎯 Архитектура (как у Discord)

```
┌─────────────────────────────┐
│  Woxly.exe (Windows App)    │
│  ┌───────────────────────┐  │
│  │  Electron (Chromium)  │  │
│  │  ┌─────────────────┐  │  │
│  │  │  React UI       │  │  │  ← Загружается из dist-vite/
│  │  │  (локально)     │  │  │
│  │  └─────────────────┘  │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
          │ HTTPS API
          ▼
┌─────────────────────────────┐
│  https://woxly.ru/api       │
│  ┌───────────────────────┐  │
│  │  Backend (Node.js)    │  │
│  │  - Auth (JWT)         │  │
│  │  - Messages           │  │
│  │  - WebRTC signaling   │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │  PostgreSQL DB        │  │
│  │  - Users, Messages    │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

**Важно:**
- ✅ UI работает локально (как Discord)
- ✅ База данных на сервере (как Discord)
- ✅ API на сервере (как Discord)

---

## 🎉 Готово!

Теперь ваше приложение работает **точно как Discord** - локальный UI, серверная база данных, нативное Windows приложение!

**Следующий шаг**: Создайте `.exe` установщик командой `npm run package` 🚀







