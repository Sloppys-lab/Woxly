# 📦 Сборка WOXLY в EXE

## 🚀 Быстрая сборка

### Вариант 1: Полная сборка (рекомендуется)
```powershell
cd C:\woxly\apps\desktop
npm run package
```

Результат: `C:\woxly\apps\desktop\dist\WOXLY Setup 1.0.0.exe`

### Вариант 2: Сборка без установщика (быстрее)
```powershell
cd C:\woxly\apps\desktop
npm run package:dir
```

Результат: `C:\woxly\apps\desktop\dist\win-unpacked\WOXLY.exe`

---

## 📋 Подробная инструкция

### Шаг 1: Подготовка
```powershell
# Убедитесь что все зависимости установлены
cd C:\woxly\apps\desktop
npm install
```

### Шаг 2: Сборка frontend
```powershell
cd C:\woxly\apps\frontend
npm run build:desktop
```

### Шаг 3: Сборка Electron
```powershell
cd C:\woxly\apps\desktop
npm run build:electron
```

### Шаг 4: Создание EXE
```powershell
# Полная сборка с установщиком
npm run package

# ИЛИ без установщика (быстрее)
npm run package:dir
```

---

## 📁 Где найти результат?

### С установщиком:
- **Путь**: `C:\woxly\apps\desktop\dist\WOXLY Setup 1.0.0.exe`
- **Размер**: ~150-200 MB
- **Описание**: Полноценный установщик для Windows

### Без установщика:
- **Путь**: `C:\woxly\apps\desktop\dist\win-unpacked\WOXLY.exe`
- **Размер**: ~150 MB (папка)
- **Описание**: Портативная версия, можно запускать сразу

---

## ⚙️ Настройка сборки

Файл `C:\woxly\apps\desktop\electron-builder.json`:

```json
{
  "appId": "com.woxly.app",
  "productName": "WOXLY",
  "directories": {
    "output": "dist",
    "buildResources": "build"
  },
  "files": [
    "dist/**/*",
    "dist-vite/**/*",
    "package.json"
  ],
  "win": {
    "target": ["nsis"],
    "icon": "build/icon.ico"
  },
  "nsis": {
    "oneClick": false,
    "allowToChangeInstallationDirectory": true,
    "createDesktopShortcut": true,
    "createStartMenuShortcut": true
  }
}
```

---

## 🎨 Иконка приложения

### Текущая иконка:
- `C:\woxly\apps\desktop\build\icon.svg`
- `C:\woxly\apps\desktop\build\icon.ico` (для Windows)

### Как изменить иконку:
1. Создайте PNG/SVG иконку (512x512 или больше)
2. Конвертируйте в ICO: https://convertio.co/png-ico/
3. Замените `C:\woxly\apps\desktop\build\icon.ico`
4. Пересоберите: `npm run package`

---

## 🐛 Решение проблем

### ❌ Ошибка: "Cannot create symbolic link"

**Проблема**: Windows требует права администратора для создания символических ссылок.

**Решение** (выберите одно):

#### 1. Включить режим разработчика (рекомендуется)
1. `Win + I` → **Система** → **Для разработчиков**
2. Включите **Режим разработчика**
3. **Перезагрузите компьютер**
4. Соберите снова: `npm run package`

#### 2. Запустить PowerShell от администратора
1. `Win + X` → **Терминал (Администратор)**
2. `cd C:\woxly\apps\desktop`
3. `npm run package`

**Подробнее**: См. `FIX_BUILD_ERROR.md`

---

### Ошибка: "Cannot find module"
```powershell
cd C:\woxly\apps\desktop
rm -rf node_modules
npm install
```

### Ошибка: "Build failed"
```powershell
# Очистите кеш
cd C:\woxly\apps\desktop
rm -rf dist
rm -rf dist-vite

# Пересоберите всё
cd C:\woxly\apps\frontend
npm run build:desktop

cd C:\woxly\apps\desktop
npm run package
```

### EXE не запускается
1. Проверьте антивирус (может блокировать)
2. Запустите от имени администратора
3. Проверьте логи: `%APPDATA%\WOXLY\logs`

---

## 📊 Размеры сборки

| Компонент | Размер |
|-----------|--------|
| Frontend (dist-vite) | ~5 MB |
| Electron | ~120 MB |
| Node modules | ~30 MB |
| **Итого** | **~155 MB** |

---

## 🚀 Автоматическая сборка

### Скрипт для быстрой сборки:
Создайте `build.ps1`:

```powershell
Write-Host "🔨 Сборка WOXLY Desktop..." -ForegroundColor Cyan

# Frontend
Write-Host "📦 Сборка frontend..." -ForegroundColor Yellow
cd C:\woxly\apps\frontend
npm run build:desktop

# Electron
Write-Host "⚡ Сборка Electron..." -ForegroundColor Yellow
cd C:\woxly\apps\desktop
npm run build:electron

# EXE
Write-Host "🎁 Создание EXE..." -ForegroundColor Yellow
npm run package

Write-Host "✅ Готово! EXE находится в: C:\woxly\apps\desktop\dist\" -ForegroundColor Green
```

Запуск:
```powershell
.\build.ps1
```

---

## 🎯 Что дальше?

### После сборки:
1. ✅ Протестируйте EXE на чистой системе
2. ✅ Проверьте все функции (вход, звонки, сообщения)
3. ✅ Создайте цифровую подпись (опционально)
4. ✅ Загрузите на сайт для скачивания

### Цифровая подпись (опционально):
```json
// electron-builder.json
{
  "win": {
    "certificateFile": "path/to/cert.pfx",
    "certificatePassword": "password",
    "signingHashAlgorithms": ["sha256"]
  }
}
```

---

## 📝 Версионирование

Изменить версию в `C:\woxly\apps\desktop\package.json`:
```json
{
  "version": "1.0.1"
}
```

Результат: `WOXLY Setup 1.0.1.exe`

---

## 🔄 Автообновление

### Как работает:
1. ✅ Проверяет обновления каждые 4 часа
2. ✅ Загружает в фоне автоматически
3. ✅ Уведомляет пользователя
4. ✅ Устанавливает при следующем запуске

### Настройка сервера:
```bash
# Создайте папку на сервере
mkdir -p /var/www/woxly.ru/downloads/desktop

# После сборки загрузите туда:
# - latest.yml
# - Woxly-Setup-X.X.X.exe
# - Woxly-Setup-X.X.X.exe.blockmap
```

### Выпуск обновления:
1. Увеличьте версию в `package.json`
2. Соберите: `npm run package`
3. Загрузите файлы из `release/` на сервер
4. Готово! Пользователи получат обновление автоматически

**Подробнее**: См. `AUTO_UPDATE_GUIDE.md`

---

## 🎉 Готово!

Теперь у вас есть полноценное Desktop приложение WOXLY для Windows!

**Особенности:**
- ✅ Локальный UI (не зависит от сервера)
- ✅ Подключение к API на woxly.ru
- ✅ Работает как Discord
- ✅ **Автообновление** (проверка каждые 4 часа)
- ✅ Нативные уведомления
- ✅ Системный трей
- ✅ Дельта-обновления (быстрее)






