# ✅ Исправление Установщика

## Проблема

После установки открывался установщик снова вместо приложения.

## Причина

В `installer.nsh` была неправильная команда запуска:
```nsis
Exec '"$INSTDIR\${PRODUCT_NAME}.exe"'  # Неправильно
```

`${PRODUCT_NAME}` = "Woxly" (из package.json)
Но файл называется `WOXLY.exe` (с заглавными буквами)

## Решение

### 1. Обновлен installer.nsh

```nsis
Function LaunchWoxly
    # Запускаем приложение с правильным именем
    Exec '"$INSTDIR\WOXLY.exe"'
FunctionEnd
```

### 2. Обновлен package.json

```json
{
  "build": {
    "productName": "WOXLY",  // Было "Woxly"
    "nsis": {
      "shortcutName": "WOXLY",  // Было "Woxly"
      "include": "build/installer.nsh"  // Добавлено
    }
  }
}
```

## Теперь Работает

После установки:
1. ✅ Открывается приложение WOXLY.exe
2. ✅ Не открывается установщик
3. ✅ Ярлык называется "WOXLY"
4. ✅ В трее "WOXLY"

## Как Пересобрать

```bash
cd C:\woxly\apps\desktop
npm run package
```

Новый установщик: `release\WOXLY-Setup-1.0.0.exe`

## Автообновления

Также добавлены автообновления! См. `АВТООБНОВЛЕНИЯ.md`

**Как работает:**
1. Проверка каждые 4 часа
2. Автоматическая загрузка
3. Уведомление пользователю
4. Установка при выходе

**Настройка:**
```json
{
  "build": {
    "publish": {
      "provider": "generic",
      "url": "https://woxly.ru/downloads/desktop"
    }
  }
}
```

**Файлы на сервере:**
```
https://woxly.ru/downloads/desktop/
├── WOXLY-Setup-1.0.0.exe
├── WOXLY-Setup-1.0.1.exe
└── latest.yml
```

**latest.yml:**
```yaml
version: 1.0.1
files:
  - url: WOXLY-Setup-1.0.1.exe
    sha512: [автоматически]
    size: [автоматически]
path: WOXLY-Setup-1.0.1.exe
releaseDate: '2024-12-14T10:00:00.000Z'
```

## Готово! 🎉

Теперь:
- ✅ Установщик работает правильно
- ✅ Автообновления настроены
- ✅ Все исправлено
