# 🔧 Исправление ошибки сборки EXE

## ❌ Проблема

При сборке возникает ошибка:
```
ERROR: Cannot create symbolic link : Клиент не обладает требуемыми правами
```

Это происходит потому что Windows требует права администратора для создания символических ссылок.

---

## ✅ Решение 1: Включить режим разработчика (рекомендуется)

### Шаг 1: Откройте настройки Windows

1. Нажмите `Win + I` для открытия настроек
2. Перейдите в **Система** → **Для разработчиков**
3. Включите **Режим разработчика**

![Режим разработчика](https://docs.microsoft.com/en-us/windows/images/devmode.png)

### Шаг 2: Перезагрузите компьютер

После включения режима разработчика **обязательно перезагрузите компьютер**.

### Шаг 3: Соберите снова

```powershell
cd C:\woxly\apps\desktop
npm run package
```

---

## ✅ Решение 2: Запустить PowerShell от администратора

### Шаг 1: Откройте PowerShell от администратора

1. Нажмите `Win + X`
2. Выберите **Windows PowerShell (Администратор)** или **Терминал (Администратор)**

### Шаг 2: Соберите приложение

```powershell
cd C:\woxly\apps\desktop
npm run package
```

---

## ✅ Решение 3: Использовать WSL (Windows Subsystem for Linux)

Если у вас установлен WSL, можно собрать там:

```bash
cd /mnt/c/woxly/apps/desktop
npm run package
```

---

## ✅ Решение 4: Очистить кеш и попробовать снова

Иногда помогает очистка кеша electron-builder:

```powershell
# Очистить кеш
Remove-Item -Path "$env:LOCALAPPDATA\electron-builder\Cache" -Recurse -Force

# Попробовать снова
cd C:\woxly\apps\desktop
npm run package
```

---

## 🎯 Какое решение выбрать?

| Решение | Плюсы | Минусы |
|---------|-------|--------|
| **Режим разработчика** | ✅ Постоянное решение<br>✅ Не нужны права админа каждый раз | ⚠️ Требует перезагрузку |
| **PowerShell от админа** | ✅ Быстро<br>✅ Не требует перезагрузку | ⚠️ Нужно запускать каждый раз |
| **WSL** | ✅ Нет проблем с правами | ⚠️ Требует установку WSL |
| **Очистка кеша** | ✅ Иногда помогает | ⚠️ Не всегда работает |

**Рекомендация**: Используйте **Решение 1** (Режим разработчика) - это самый надёжный способ.

---

## 📝 После успешной сборки

После того как сборка пройдёт успешно, вы найдёте файлы здесь:

```
C:\woxly\apps\desktop\release\
├── Woxly-Setup-1.0.0.exe          # Установщик
├── Woxly-Setup-1.0.0.exe.blockmap # Для дельта-обновлений
└── latest.yml                      # Информация о версии
```

---

## 🚀 Быстрая команда для включения режима разработчика

Можно включить через PowerShell (от администратора):

```powershell
# Включить режим разработчика
reg add "HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock" /t REG_DWORD /f /v "AllowDevelopmentWithoutDevLicense" /d "1"

# Перезагрузить
Restart-Computer
```

---

## ❓ Часто задаваемые вопросы

### Q: Безопасно ли включать режим разработчика?
**A**: Да, это официальная функция Windows для разработчиков. Она просто даёт дополнительные права для разработки.

### Q: Можно ли собрать без этих прав?
**A**: К сожалению, нет. electron-builder требует права для создания символических ссылок.

### Q: Почему это происходит?
**A**: electron-builder скачивает инструменты для подписи кода (winCodeSign), которые содержат символические ссылки для macOS. Windows не может их распаковать без специальных прав.

### Q: Можно ли отключить подпись кода?
**A**: Мы уже отключили (`"sign": null`), но electron-builder всё равно пытается скачать инструменты.

---

## 🎉 Готово!

После применения одного из решений сборка должна пройти успешно!

Если проблема сохраняется, напишите мне - найдём другое решение.






