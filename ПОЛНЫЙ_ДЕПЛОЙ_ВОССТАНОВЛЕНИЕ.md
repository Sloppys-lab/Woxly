# 🚀 Полный Деплой: Восстановление Пароля

## ⚠️ Важно!

Нужно обновить **И backend И frontend**, потому что backend еще не имеет эндпоинтов для восстановления пароля!

---

## 📦 Файлы для Загрузки на Сервер

### Backend (3 файла):
```
1. C:\woxly\apps\backend\src\controllers\auth.ts
   → /root/woxly/apps/backend/src/controllers/auth.ts

2. C:\woxly\apps\backend\src\routes\auth.ts
   → /root/woxly/apps/backend/src/routes/auth.ts

3. C:\woxly\apps\backend\src\utils\email.ts
   → /root/woxly/apps/backend/src/utils/email.ts
```

### Frontend (1 файл):
```
4. C:\woxly\apps\frontend\src\pages\auth\ForgotPasswordPage.tsx
   → /root/woxly/apps/frontend/src/pages/auth/ForgotPasswordPage.tsx
```

---

## 📋 Пошаговая Инструкция

### Шаг 1: Загрузите Файлы (FileZilla/WinSCP)

1. Откройте FileZilla или WinSCP
2. Подключитесь к серверу (IP, root, пароль)
3. Перетащите все 4 файла в соответствующие папки

### Шаг 2: Подключитесь через SSH (Termius)

```bash
ssh root@ваш-ip
```

### Шаг 3: Обновите Backend

```bash
cd /root/woxly/apps/backend

# Пересоберите TypeScript
npm run build

# Перезапустите backend
pm2 restart backend

# Проверьте логи
pm2 logs backend --lines 20
```

### Шаг 4: Обновите Frontend

```bash
cd /root/woxly/apps/frontend

# Пересоберите
npm run build

# Перезапустите Nginx
sudo systemctl reload nginx
```

### Шаг 5: Проверьте

Откройте в браузере:
```
https://woxly.ru/#/auth/login
```

Нажмите "Забыли пароль?" и попробуйте отправить код.

---

## ⚡ Быстрые Команды (После Загрузки Файлов)

### Одной командой:

```bash
cd /root/woxly && \
cd apps/backend && npm run build && pm2 restart backend && \
cd ../frontend && npm run build && \
sudo systemctl reload nginx && \
echo "✅ Деплой завершен!"
```

---

## 🔍 Проверка Backend API

После деплоя проверьте что эндпоинты работают:

```bash
# Проверка 1: Запрос восстановления
curl -X POST http://localhost:3001/api/auth/request-password-reset \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com"}'

# Должен вернуть:
# {"message":"Если email зарегистрирован, на него отправлено письмо с кодом восстановления"}

# Проверка 2: Логи backend
pm2 logs backend --lines 30
```

---

## ✅ Что Должно Работать

После деплоя:

1. **Страница восстановления:**
   - https://woxly.ru/#/auth/forgot-password
   - Правильный стиль (gradient "WOXLY")
   - 6 полей для кода

2. **API эндпоинты:**
   - POST `/api/auth/request-password-reset` - отправка кода
   - POST `/api/auth/reset-password` - сброс пароля

3. **Email:**
   - Письмо с 6-значным кодом
   - Красивый дизайн

4. **Поток:**
   - Email → Код → Новый пароль → Успех

---

## 🐛 Решение Проблем

### Ошибка "Route not found"

Это значит backend не обновлен. Проверьте:

```bash
# Проверьте что файлы загружены
ls -la /root/woxly/apps/backend/src/controllers/auth.ts
ls -la /root/woxly/apps/backend/src/routes/auth.ts

# Пересоберите backend
cd /root/woxly/apps/backend
npm run build

# Перезапустите
pm2 restart backend

# Проверьте логи
pm2 logs backend --lines 50
```

### Backend не запускается

```bash
# Проверьте ошибки
pm2 logs backend --lines 50

# Возможно нужно установить зависимости
cd /root/woxly/apps/backend
npm install

# Пересоберите
npm run build

# Перезапустите
pm2 restart backend
```

### Email не отправляется

Проверьте `.env` файл:

```bash
cat /root/woxly/apps/backend/.env | grep SMTP

# Должно быть:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@woxly.com
```

Для Gmail нужен App Password:
1. Включите 2FA в Google
2. Создайте App Password: https://myaccount.google.com/apppasswords
3. Используйте этот пароль в `SMTP_PASS`

---

## 📝 Изменения в Коде

### Backend:

**`auth.ts` (controllers):**
- Добавлены функции `requestPasswordReset` и `resetPassword`
- Генерация 6-значного кода
- Отправка email через `sendVerificationEmail`

**`auth.ts` (routes):**
- Добавлены роуты `/request-password-reset` и `/reset-password`

**`email.ts` (utils):**
- Улучшен шаблон email
- Добавлен параметр `purpose` для разных типов писем

### Frontend:

**`ForgotPasswordPage.tsx`:**
- Правильный стиль (как LoginPage)
- 6 отдельных полей для кода
- Автофокус на следующее поле
- Кнопка "Отправить код снова" на странице ввода кода
- Правильные API эндпоинты

---

## 🎉 Готово!

После деплоя восстановление пароля будет полностью работать!

**Время деплоя:** ~5 минут

**Проверка:**
1. https://woxly.ru/#/auth/login
2. "Забыли пароль?"
3. Введите email
4. Получите код на почту
5. Введите код
6. Создайте новый пароль
7. Войдите с новым паролем

Готово! 🚀
