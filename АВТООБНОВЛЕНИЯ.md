# 🔄 Автообновления в Woxly

## Как Это Работает

### 1. **Архитектура**

```
Desktop App (Electron)
    ↓
electron-updater (библиотека)
    ↓
Проверка обновлений на сервере
    ↓
Скачивание новой версии
    ↓
Установка при выходе
```

### 2. **Процесс Автообновления**

#### Шаг 1: Проверка Обновлений
```typescript
// В main.ts
autoUpdater.checkForUpdates();
```

Приложение проверяет сервер каждые 4 часа:
- Первая проверка через 10 секунд после запуска
- Затем каждые 4 часа автоматически

#### Шаг 2: Обновление Найдено
```
🎉 Уведомление: "Доступно обновление"
   "Новая версия X.X.X загружается..."
```

Приложение автоматически начинает скачивать обновление в фоне.

#### Шаг 3: Скачивание
```
Загружено 0%...
Загружено 50%...
Загружено 100%
```

Обновление скачивается в фоне, не мешая работе.

#### Шаг 4: Готово к Установке
```
✅ Уведомление: "Обновление готово"
   "Версия X.X.X готова к установке. Нажмите для перезапуска."
```

Пользователь может:
- Нажать на уведомление → установка сразу
- Закрыть приложение → установка при выходе

---

## 📝 Настройка Сервера для Обновлений

### Вариант 1: GitHub Releases (Рекомендуется)

#### 1. Создайте GitHub Release

```bash
# 1. Соберите приложение
cd C:\woxly\apps\desktop
npm run package

# 2. Файлы для загрузки на GitHub:
# - Woxly-Setup-1.0.0.exe (установщик)
# - latest.yml (информация о версии)
```

#### 2. Обновите package.json

```json
{
  "build": {
    "publish": {
      "provider": "github",
      "owner": "your-username",
      "repo": "woxly"
    }
  }
}
```

#### 3. Создайте GitHub Token

1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. Выберите scope: `repo`
4. Скопируйте token

#### 4. Опубликуйте Release

```bash
# Установите переменную окружения
$env:GH_TOKEN="your-github-token"

# Соберите и опубликуйте
npm run package
```

---

### Вариант 2: Свой Сервер (Текущий)

#### 1. Структура на Сервере

```
https://woxly.ru/downloads/desktop/
├── Woxly-Setup-1.0.0.exe
├── Woxly-Setup-1.0.1.exe
├── Woxly-Setup-1.0.2.exe
└── latest.yml
```

#### 2. Файл latest.yml

```yaml
version: 1.0.2
files:
  - url: Woxly-Setup-1.0.2.exe
    sha512: [хеш файла]
    size: 85123456
path: Woxly-Setup-1.0.2.exe
sha512: [хеш файла]
releaseDate: '2024-12-14T10:00:00.000Z'
```

#### 3. Настройка Nginx

```nginx
# /etc/nginx/sites-available/woxly
server {
    listen 80;
    server_name woxly.ru;

    # Папка для обновлений
    location /downloads/desktop/ {
        alias /var/www/woxly/downloads/desktop/;
        autoindex on;
        
        # CORS для автообновлений
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods 'GET, HEAD, OPTIONS';
    }
}
```

#### 4. Загрузка Обновлений на Сервер

```bash
# На вашем компьютере
cd C:\woxly\apps\desktop\release

# Загрузите через FileZilla/WinSCP:
# Woxly-Setup-1.0.0.exe → /var/www/woxly/downloads/desktop/
# latest.yml → /var/www/woxly/downloads/desktop/
```

---

## 🔧 Настройка в Коде

### main.ts (Уже Настроено)

```typescript
// Проверка обновлений
function checkForUpdates() {
  if (process.env.NODE_ENV !== 'development') {
    // Настройки
    autoUpdater.autoDownload = true;        // Автоматическая загрузка
    autoUpdater.autoInstallOnAppQuit = true; // Установка при выходе
    
    // Проверка каждые 4 часа
    setInterval(() => {
      autoUpdater.checkForUpdates();
    }, 4 * 60 * 60 * 1000);
    
    // Первая проверка через 10 секунд
    setTimeout(() => {
      autoUpdater.checkForUpdates();
    }, 10000);
  }
}
```

### События Автообновления

```typescript
// 1. Обновление найдено
autoUpdater.on('update-available', (info) => {
  // Показываем уведомление
  new Notification({
    title: '🎉 Доступно обновление',
    body: `Новая версия ${info.version} загружается...`
  }).show();
});

// 2. Прогресс загрузки
autoUpdater.on('download-progress', (progressObj) => {
  console.log(`Загружено ${progressObj.percent}%`);
});

// 3. Обновление загружено
autoUpdater.on('update-downloaded', (info) => {
  // Показываем уведомление
  const notification = new Notification({
    title: '✅ Обновление готово',
    body: `Версия ${info.version} готова к установке`
  });
  
  // При клике - установить сразу
  notification.on('click', () => {
    autoUpdater.quitAndInstall(false, true);
  });
});

// 4. Ошибка
autoUpdater.on('error', (error) => {
  console.error('Ошибка обновления:', error);
});
```

---

## 📦 Процесс Выпуска Обновления

### Шаг 1: Обновите Версию

```json
// package.json
{
  "version": "1.0.1"  // Было 1.0.0
}
```

### Шаг 2: Соберите Приложение

```bash
cd C:\woxly\apps\desktop
npm run package
```

### Шаг 3: Загрузите на Сервер

```bash
# Файлы из release/:
# - Woxly-Setup-1.0.1.exe
# - latest.yml

# Загрузите через FileZilla в:
# /var/www/woxly/downloads/desktop/
```

### Шаг 4: Пользователи Получат Обновление

```
Через 10 секунд - 4 часа:
  ↓
Проверка обновлений
  ↓
Найдена версия 1.0.1
  ↓
Автоматическая загрузка
  ↓
Уведомление "Готово к установке"
  ↓
Установка при выходе
```

---

## 🎯 Как Пользователь Видит Обновление

### Сценарий 1: Автоматическая Установка

```
1. Пользователь работает в приложении
2. Появляется уведомление: "Доступно обновление"
3. Обновление скачивается в фоне
4. Появляется уведомление: "Обновление готово"
5. Пользователь закрывает приложение
6. Обновление устанавливается автоматически
7. Приложение запускается с новой версией
```

### Сценарий 2: Ручная Установка

```
1. Пользователь работает в приложении
2. Появляется уведомление: "Доступно обновление"
3. Обновление скачивается в фоне
4. Появляется уведомление: "Обновление готово"
5. Пользователь НАЖИМАЕТ на уведомление
6. Приложение закрывается и устанавливает обновление
7. Приложение запускается с новой версией
```

---

## 🔍 Проверка Автообновлений

### Тест 1: Проверка Сервера

```bash
# Проверьте что файлы доступны
curl -I https://woxly.ru/downloads/desktop/latest.yml
curl -I https://woxly.ru/downloads/desktop/Woxly-Setup-1.0.0.exe
```

### Тест 2: Ручная Проверка

В приложении откройте DevTools (F12):

```javascript
// Проверить текущую версию
console.log(await window.electron.getAppVersion());

// Принудительно проверить обновления
await window.electron.checkForUpdates();
```

### Тест 3: Логи

```bash
# На сервере
tail -f /var/log/nginx/access.log | grep downloads

# В приложении (DevTools)
# Ищите логи: [UPDATER] ...
```

---

## 📊 Статистика Обновлений

### Мониторинг на Сервере

```bash
# Сколько раз скачали latest.yml (проверка обновлений)
grep "latest.yml" /var/log/nginx/access.log | wc -l

# Сколько раз скачали установщик (реальные обновления)
grep "Woxly-Setup" /var/log/nginx/access.log | wc -l
```

---

## ⚙️ Настройки Автообновления

### Изменить Частоту Проверки

```typescript
// main.ts
// Вместо 4 часов (по умолчанию):
setInterval(() => {
  autoUpdater.checkForUpdates();
}, 4 * 60 * 60 * 1000); // 4 часа

// Можно изменить на:
// 1 час:   1 * 60 * 60 * 1000
// 6 часов: 6 * 60 * 60 * 1000
// 12 часов: 12 * 60 * 60 * 1000
// 24 часа: 24 * 60 * 60 * 1000
```

### Отключить Автоматическую Загрузку

```typescript
autoUpdater.autoDownload = false; // Спрашивать перед загрузкой

// Добавить подтверждение
autoUpdater.on('update-available', (info) => {
  dialog.showMessageBox({
    type: 'info',
    title: 'Доступно обновление',
    message: `Версия ${info.version} доступна. Скачать?`,
    buttons: ['Да', 'Позже']
  }).then((result) => {
    if (result.response === 0) {
      autoUpdater.downloadUpdate();
    }
  });
});
```

---

## 🎉 Готово!

Автообновления настроены и работают автоматически!

**Что происходит:**
1. ✅ Проверка каждые 4 часа
2. ✅ Автоматическая загрузка
3. ✅ Уведомления пользователю
4. ✅ Установка при выходе
5. ✅ Без участия пользователя

**Для выпуска обновления:**
1. Измените версию в `package.json`
2. Соберите: `npm run package`
3. Загрузите на сервер: `Woxly-Setup-X.X.X.exe` и `latest.yml`
4. Готово! Пользователи получат обновление автоматически

🚀
