# 🪟 Woxly Desktop для Windows

Десктопное приложение Woxly для Windows, созданное на Electron.

## 🚀 Быстрый старт

### Разработка

```bash
cd apps/desktop
npm install
npm run dev
```

### Сборка для Windows

```bash
# Полная сборка с установщиком
npm run dist

# Только сборка без упаковки
npm run build

# Портативная версия
npm run package
```

## 📦 Что будет создано

После сборки в папке `release/` появятся:

1. **Woxly-Setup-1.0.0.exe** - Установщик (NSIS)
   - Размер: ~150-200 MB
   - Создает ярлыки на рабочем столе и в меню Пуск
   - Автообновления

2. **Woxly-Portable-1.0.0.exe** - Портативная версия
   - Размер: ~150-200 MB
   - Не требует установки
   - Можно запускать с флешки

## ✨ Возможности

### Нативные функции Windows
- ✅ **Системный трей** - сворачивание в трей
- ✅ **Уведомления** - нативные Windows уведомления
- ✅ **Автозапуск** - запуск с Windows
- ✅ **Горячие клавиши** - глобальные хоткеи
- ✅ **Автообновления** - автоматическое обновление приложения

### Интеграция с системой
- Работает в фоне
- Минимальное потребление ресурсов
- Быстрый запуск
- Нативный внешний вид

## 🎯 Использование

### Основные команды

```bash
# Разработка
npm run dev              # Запуск в режиме разработки
npm run dev:electron     # Только Electron
npm run dev:vite         # Только Vite

# Сборка
npm run build            # Сборка проекта
npm run build:vite       # Сборка Vite
npm run build:electron   # Сборка Electron

# Упаковка
npm run package          # Создать установщик
npm run package:dir      # Создать папку без упаковки
npm run dist             # Полная сборка и упаковка

# Запуск
npm start                # Запуск собранного приложения
```

## 📁 Структура проекта

```
apps/desktop/
├── src/
│   ├── main.ts          # Главный процесс Electron
│   └── preload.ts       # Preload скрипт (bridge)
├── build/
│   ├── icon.ico         # Иконка приложения
│   └── icon.png         # PNG иконка
├── dist/                # Скомпилированный Electron
├── dist-vite/           # Скомпилированный Vite
├── release/             # Готовые установщики
├── package.json
├── tsconfig.electron.json
└── README.md
```

## 🔧 Конфигурация

### Изменение URL сервера

В `src/main.ts`:

```typescript
const SERVER_URL = 'https://woxly.ru';
```

### Настройка автообновлений

В `package.json` добавьте:

```json
"publish": {
  "provider": "github",
  "owner": "your-username",
  "repo": "woxly"
}
```

### Изменение иконки

Замените файлы в папке `build/`:
- `icon.ico` - для Windows (256x256)
- `icon.png` - для трея (512x512)

## 🎨 Кастомизация

### Размер окна

В `src/main.ts`:

```typescript
mainWindow = new BrowserWindow({
  width: 1400,  // Ширина
  height: 900,  // Высота
  minWidth: 1000,
  minHeight: 700,
});
```

### Автозапуск по умолчанию

В `src/main.ts`:

```typescript
const autoLaunch = store.get('autoLaunch', true); // true = включен
```

## 🐛 Отладка

### Открыть DevTools

В режиме разработки DevTools открываются автоматически.

Для продакшена добавьте в `src/main.ts`:

```typescript
mainWindow.webContents.openDevTools();
```

### Логи

Логи находятся в:
```
%APPDATA%\Woxly\logs\
```

## 📦 Публикация

### GitHub Releases

1. Создайте GitHub Release
2. Загрузите файлы из `release/`
3. Пользователи получат автообновление

### Microsoft Store

1. Создайте аккаунт разработчика
2. Используйте `electron-windows-store`
3. Опубликуйте через Partner Center

## 🔒 Безопасность

- ✅ `contextIsolation: true` - изоляция контекста
- ✅ `nodeIntegration: false` - отключен Node в рендере
- ✅ `webSecurity: true` - включена веб-безопасность
- ✅ Preload скрипт для безопасного API

## 🚀 Оптимизация

### Уменьшение размера

1. Используйте `electron-builder` с `asar`
2. Включите сжатие
3. Удалите неиспользуемые зависимости

### Ускорение запуска

1. Используйте V8 snapshots
2. Отложенная загрузка модулей
3. Кэширование

## 📝 Changelog

### v1.0.0 (2024)
- ✨ Первый релиз
- ✅ Системный трей
- ✅ Автообновления
- ✅ Нативные уведомления
- ✅ Автозапуск

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте логи
2. Откройте issue на GitHub
3. Напишите в поддержку

## 📄 Лицензия

MIT License - см. LICENSE.txt









