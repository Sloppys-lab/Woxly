# 🔧 Исправление иконки установщика

Если иконка установщика не обновилась, выполните следующие шаги:

## 🚀 Быстрое решение (рекомендуется)

Запустите автоматический скрипт:

```powershell
cd C:\woxly\apps\desktop
.\fix-installer-icon.ps1
npm run package
```

## 📋 Пошаговая инструкция

### Шаг 1: Пересоздайте иконки

```powershell
cd C:\woxly\apps\desktop
npm run icons
```

Это пересоздаст `icon.png` и `icon.ico` из `build/icon.svg` с новой иконкой.

### Шаг 2: Очистите кеш electron-builder

```powershell
cd C:\woxly\apps\desktop
Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force release -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force node_modules\.cache -ErrorAction SilentlyContinue
```

### Шаг 3: Пересоберите проект

```powershell
cd C:\woxly\apps\desktop
npm run package
```

## 🔍 Проверка

После сборки:

1. **Проверьте файл установщика:**
   - Откройте `release\Woxly-Setup-1.0.0.exe` в проводнике Windows
   - Иконка должна быть с новой иконкой (звуковые волны и буква W)

2. **Проверьте после установки:**
   - Установите приложение
   - Ярлык на рабочем столе должен иметь новую иконку
   - Иконка в меню Пуск также должна быть новой

## ⚠️ Если проблема сохраняется

### Windows кеширует иконки установщиков

Windows может кешировать иконки установщиков. Попробуйте:

1. **Очистить кеш иконок Windows:**
   ```powershell
   # Закройте все окна проводника
   # Затем выполните:
   Remove-Item "$env:LOCALAPPDATA\IconCache.db" -ErrorAction SilentlyContinue
   Remove-Item "$env:LOCALAPPDATA\Microsoft\Windows\Explorer\iconcache*" -Recurse -Force -ErrorAction SilentlyContinue
   # Перезапустите проводник (или перезагрузите компьютер)
   ```

2. **Проверьте файл icon.ico:**
   ```powershell
   cd C:\woxly\apps\desktop\build
   # Проверьте дату изменения файла
   Get-Item icon.ico | Select-Object Name, LastWriteTime, Length
   ```

3. **Убедитесь, что ICO файл правильный:**
   - Откройте `build/icon.ico` в любом просмотрщике изображений
   - Должна быть видна новая иконка со звуковыми волнами

4. **Попробуйте переименовать установщик:**
   - Измените версию в `package.json`
   - Или переименуйте файл после сборки

## 📝 Дополнительная информация

- **Путь к иконке в package.json:** `"icon": "build/icon.ico"`
- **Иконка установщика:** `"installerIcon": "build/icon.ico"`
- **Иконка деинсталлятора:** `"uninstallerIcon": "build/icon.ico"`

Все эти пути должны указывать на один и тот же файл `build/icon.ico`.




