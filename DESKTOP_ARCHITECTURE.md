# 🖥️ Woxly Desktop - Автономное приложение

## 📌 Архитектура

```
┌─────────────────────────────────────┐
│   Woxly Desktop (Windows .exe)      │
│                                     │
│  ┌─────────────────────────────┐   │
│  │   React UI (локальные файлы)│   │
│  │   - Весь интерфейс          │   │
│  │   - Компоненты              │   │
│  │   - Логика                  │   │
│  └──────────┬──────────────────┘   │
│             │                       │
│             │ HTTPS запросы         │
│             ▼                       │
│  ┌─────────────────────────────┐   │
│  │ API клиент                  │   │
│  │ → https://woxly.ru/api      │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
           │
           │ Только база данных
           ▼
┌─────────────────────────────────────┐
│   Сервер (woxly.ru)                 │
│   - База данных PostgreSQL          │
│   - API endpoints                   │
│   - WebSocket для звонков           │
└─────────────────────────────────────┘
```

## ✅ Преимущества

### Desktop приложение:
- ✅ **Работает локально** - весь UI на вашем ПК
- ✅ **Быстрая загрузка** - нет загрузки сайта
- ✅ **Работает без интернета** (UI) - можно открыть и посмотреть
- ✅ **Нативные возможности** - системный трей, уведомления, автозапуск
- ✅ **Меньше нагрузка на сервер** - не отдаем HTML/CSS/JS при каждом запуске
- ✅ **Обновления UI** - только при обновлении .exe

### Подключение к серверу:
- 🌐 **Только API запросы** - авторизация, сообщения, друзья
- 🌐 **WebSocket** - для голосовых звонков и реального времени
- 🌐 **База данных** - на сервере, одна для всех

## 🔧 Как это работает

### 1. Запуск приложения
```
Пользователь запускает Woxly.exe
  ↓
Electron загружает локальные файлы (dist-vite/index.html)
  ↓
React приложение запускается на ПК
  ↓
Подключается к https://woxly.ru/api для данных
```

### 2. Работа с данными
```
Действие пользователя (отправить сообщение)
  ↓
React компонент вызывает API
  ↓
HTTPS запрос → https://woxly.ru/api/messages
  ↓
Сервер сохраняет в базу данных
  ↓
Ответ возвращается в приложение
```

### 3. Голосовые звонки
```
Пользователь звонит
  ↓
WebSocket подключение к woxly.ru
  ↓
WebRTC peer-to-peer между пользователями
  ↓
Сервер только координирует (signaling)
```

## 🚀 Сборка приложения

### Разработка (с hot reload)
```bash
cd c:\woxly\apps\desktop
npm run dev
```
Это запустит:
- Vite dev сервер на localhost:5173 (с hot reload)
- Electron приложение которое загружает его

### Production build
```bash
cd c:\woxly\apps\desktop
npm run build
```
Это:
1. Соберет фронтенд → `apps/desktop/dist-vite/`
2. Скомпилирует Electron → `apps/desktop/dist/`

### Создание .exe файла
```bash
cd c:\woxly\apps\desktop
npm run package
```
Результат: `apps/desktop/release/Woxly-Setup-1.0.0.exe`

## 📦 Что включено в .exe

```
Woxly-Setup-1.0.0.exe (установщик)
  │
  ├─ Electron runtime (~150 MB)
  ├─ Ваш React UI (dist-vite/) (~2-5 MB)
  ├─ Node.js модули (electron-store, electron-updater)
  └─ Иконки и ресурсы
```

**Общий размер**: ~150-180 MB

## 🔑 Конфигурация API

API URL захардкожен в `vite.config.desktop.ts`:

```typescript
define: {
  'import.meta.env.VITE_API_URL': JSON.stringify('https://woxly.ru/api'),
}
```

Это значит что все Desktop приложения подключаются к вашему серверу!

## 🎯 Что работает offline

### ✅ Работает без интернета:
- Открыть приложение
- Посмотреть интерфейс
- Посмотреть кешированные данные (если есть)

### ❌ НЕ работает без интернета:
- Авторизация
- Отправка/получение сообщений
- Голосовые звонки
- Синхронизация друзей
- Любые действия требующие базу данных

## 📱 Одновременное использование

Пользователь может использовать:
- ✅ Desktop приложение
- ✅ Web версию в браузере (woxly.ru)
- ✅ Оба одновременно!

Все используют **одну базу данных** → всё синхронизируется.

## 🔄 Обновления

### Автоматические обновления UI:
- ❌ НЕТ - UI встроен в .exe
- Нужно пересобрать .exe и выпустить обновление

### Автоматические обновления приложения:
- ✅ ДА - `electron-updater` проверяет обновления
- Пользователь получит уведомление о новой версии
- Можно настроить автоматическую установку

## 🎨 Кастомизация

### Изменить иконку:
```bash
# Поместите icon.ico в apps/desktop/build/
apps/desktop/build/icon.ico
```

### Изменить название:
```json
// apps/desktop/package.json
"build": {
  "productName": "Woxly",
  "appId": "com.woxly.desktop"
}
```

### Изменить API URL:
```typescript
// apps/frontend/vite.config.desktop.ts
define: {
  'import.meta.env.VITE_API_URL': JSON.stringify('https://your-server.com/api'),
}
```

## 🐛 Отладка

### Открыть DevTools:
- В разработке: автоматически открыты
- В продакшене: `Ctrl+Shift+I` (если не отключено)

### Логи:
```bash
# Windows
%APPDATA%\Woxly\logs\
```

### Проверить сборку:
```bash
npm start  # Запустить без сборки .exe
```

## 📊 Сравнение

| Функция | Web версия | Desktop версия |
|---------|-----------|----------------|
| Установка | Не нужна | Нужна (один раз) |
| Обновления UI | Автоматические | Нужна новая версия |
| Размер | ~2-3 MB (кеш браузера) | ~180 MB (с runtime) |
| Скорость загрузки | Зависит от интернета | Мгновенная |
| Системный трей | ❌ | ✅ |
| Автозапуск | ❌ | ✅ |
| Уведомления | Ограничены | Нативные |
| Offline UI | ❌ | ✅ |
| API/БД | ✅ | ✅ (одинаково) |

## 🎉 Итог

Теперь у вас **два способа** использовать Woxly:

1. **Web** (woxly.ru) - для быстрого доступа
2. **Desktop** (.exe) - для постоянного использования

Оба используют **одну базу данных** на сервере! 🚀







