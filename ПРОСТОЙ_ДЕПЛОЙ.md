# 🚀 Простой Деплой - Пошагово

## Что Нужно Обновить на Сервере

### ✅ Backend (3 файла):
1. `apps/backend/src/controllers/auth.ts`
2. `apps/backend/src/routes/auth.ts`
3. `apps/backend/src/utils/email.ts`

### ✅ Frontend (3 файла):
1. `apps/frontend/src/App.tsx`
2. `apps/frontend/src/pages/auth/LoginPage.tsx`
3. `apps/frontend/src/pages/auth/ForgotPasswordPage.tsx` (новый файл)

### ❌ Desktop - НЕ НУЖНО на сервер!

---

## 📋 Вариант 1: Через FileZilla / WinSCP (Проще всего)

### Шаг 1: Откройте FileZilla или WinSCP

### Шаг 2: Подключитесь к серверу
- **Хост:** ваш IP сервера
- **Пользователь:** root (или ваш пользователь)
- **Пароль:** ваш пароль
- **Порт:** 22

### Шаг 3: Загрузите файлы

**Backend файлы:**
```
Локально: C:\woxly\apps\backend\src\controllers\auth.ts
На сервере: /root/woxly/apps/backend/src/controllers/auth.ts

Локально: C:\woxly\apps\backend\src\routes\auth.ts
На сервере: /root/woxly/apps/backend/src/routes/auth.ts

Локально: C:\woxly\apps\backend\src\utils\email.ts
На сервере: /root/woxly/apps/backend/src/utils/email.ts
```

**Frontend файлы:**
```
Локально: C:\woxly\apps\frontend\src\App.tsx
На сервере: /root/woxly/apps/frontend/src/App.tsx

Локально: C:\woxly\apps\frontend\src\pages\auth\LoginPage.tsx
На сервере: /root/woxly/apps/frontend/src/pages/auth/LoginPage.tsx

Локально: C:\woxly\apps\frontend\src\pages\auth\ForgotPasswordPage.tsx
На сервере: /root/woxly/apps/frontend/src/pages/auth/ForgotPasswordPage.tsx
```

### Шаг 4: Подключитесь к серверу через SSH (Termius)

### Шаг 5: Выполните команды на сервере

```bash
# Перейдите в папку проекта
cd /root/woxly

# Пересоберите backend
cd apps/backend
npm run build

# Пересоберите frontend
cd ../frontend
npm run build

# Перезапустите приложения
pm2 restart all

# Проверьте статус
pm2 status
pm2 logs backend --lines 20
```

### Шаг 6: Проверьте сайт

Откройте в браузере:
- https://woxly.ru/#/auth/login
- Должна быть ссылка "Забыли пароль?"

---

## 📋 Вариант 2: Через Git (Если настроен)

### На вашем компьютере:

```powershell
cd C:\woxly

# Закоммитьте изменения
git add .
git commit -m "Добавлено восстановление пароля"
git push origin main
```

### На сервере:

```bash
cd /root/woxly

# Остановите приложения
pm2 stop all

# Обновите код
git pull origin main

# Пересоберите backend
cd apps/backend
npm run build

# Пересоберите frontend
cd ../frontend
npm run build

# Запустите приложения
cd /root/woxly
pm2 restart all

# Проверьте
pm2 status
pm2 logs backend --lines 20
```

---

## 📋 Вариант 3: Автоматический Скрипт

### Шаг 1: Запустите скрипт на вашем компьютере

```powershell
cd C:\woxly
.\deploy-to-server.ps1
```

### Шаг 2: Следуйте инструкциям скрипта

Скрипт создаст архив и покажет команды для деплоя.

---

## ⚡ Быстрая Команда (Все в Одном)

Если у вас уже загружены файлы на сервер:

```bash
cd /root/woxly && \
pm2 stop all && \
cd apps/backend && npm run build && \
cd ../frontend && npm run build && \
cd /root/woxly && pm2 restart all && \
pm2 status
```

---

## ✅ Проверка После Деплоя

### 1. Проверьте PM2

```bash
pm2 status

# Должно быть:
# ┌─────┬────────────┬─────────┬─────────┐
# │ id  │ name       │ status  │ restart │
# ├─────┼────────────┼─────────┼─────────┤
# │ 0   │ backend    │ online  │ 0       │
# │ 1   │ frontend   │ online  │ 0       │
# └─────┴────────────┴─────────┴─────────┘
```

### 2. Проверьте Логи

```bash
pm2 logs backend --lines 20

# Не должно быть ошибок
```

### 3. Проверьте Сайт

Откройте браузер:
1. https://woxly.ru/#/auth/login
2. Нажмите "Забыли пароль?"
3. Должна открыться страница восстановления

### 4. Проверьте API

```bash
curl -X POST http://localhost:3001/api/auth/request-password-reset \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com"}'

# Должен вернуть:
# {"message":"Если email зарегистрирован, на него отправлено письмо с кодом восстановления"}
```

---

## ❌ Если Что-то Не Работает

### Backend не запускается

```bash
# Проверьте ошибки
pm2 logs backend --lines 50

# Пересоберите
cd /root/woxly/apps/backend
rm -rf dist
npm run build
pm2 restart backend
```

### Frontend не обновился

```bash
# Очистите кеш браузера: Ctrl+F5

# Пересоберите frontend
cd /root/woxly/apps/frontend
rm -rf dist
npm run build

# Перезапустите Nginx
sudo systemctl reload nginx
```

### Страница 404

```bash
# Проверьте Nginx конфиг
cat /etc/nginx/sites-available/woxly

# Перезапустите Nginx
sudo nginx -t
sudo systemctl reload nginx
```

---

## 📝 Что НЕ Нужно Деплоить

❌ **Desktop приложение** (`apps/desktop/*`)
- Это только для Windows компьютеров пользователей
- Собирается отдельно: `npm run package`
- Установщик: `Woxly-Setup-1.0.0.exe`

❌ **Installer** (`apps/installer/*`)
- Это только для создания установщика

❌ **Документация** (`*.md`)
- Опционально, можно не загружать

---

## 🎯 Итого

**Нужно обновить:**
- ✅ 3 файла в backend
- ✅ 3 файла в frontend
- ✅ Пересобрать backend: `npm run build`
- ✅ Пересобрать frontend: `npm run build`
- ✅ Перезапустить: `pm2 restart all`

**Время деплоя:** ~5 минут

**Готово!** 🚀
