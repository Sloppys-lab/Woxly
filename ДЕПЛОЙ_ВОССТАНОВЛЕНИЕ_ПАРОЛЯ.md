# 🚀 Деплой: Восстановление Пароля на Сервер

## 📦 Что Нужно Загрузить на Сервер

**Только 1 файл:**
- `apps/frontend/src/pages/auth/ForgotPasswordPage.tsx`

**НЕ нужно:**
- ❌ `apps/desktop/src/main.ts` - это только для desktop приложения

---

## 📋 Вариант 1: Через FileZilla/WinSCP (Проще)

### Шаг 1: Откройте FileZilla/WinSCP

### Шаг 2: Подключитесь к серверу
- **Хост:** ваш IP
- **Пользователь:** root
- **Пароль:** ваш пароль

### Шаг 3: Загрузите файл

```
Локально:
C:\woxly\apps\frontend\src\pages\auth\ForgotPasswordPage.tsx

На сервере:
/root/woxly/apps/frontend/src/pages/auth/ForgotPasswordPage.tsx
```

### Шаг 4: Подключитесь через SSH (Termius)

```bash
ssh root@ваш-ip
```

### Шаг 5: Пересоберите frontend

```bash
cd /root/woxly/apps/frontend
npm run build
sudo systemctl reload nginx
```

### Шаг 6: Проверьте

Откройте в браузере:
```
https://woxly.ru/#/auth/login
```

Нажмите "Забыли пароль?" - должна открыться красивая страница восстановления.

---

## 📋 Вариант 2: Через Git (Если Настроен)

### На вашем компьютере:

```powershell
cd C:\woxly

git add apps/frontend/src/pages/auth/ForgotPasswordPage.tsx
git commit -m "Исправлена страница восстановления пароля"
git push origin main
```

### На сервере:

```bash
cd /root/woxly
git pull origin main
cd apps/frontend
npm run build
sudo systemctl reload nginx
```

---

## ⚡ Быстрая Команда (Одной Строкой)

После загрузки файла на сервер:

```bash
cd /root/woxly/apps/frontend && npm run build && sudo systemctl reload nginx && echo "✅ Готово!"
```

---

## ✅ Проверка После Деплоя

### 1. Откройте сайт:
```
https://woxly.ru/#/auth/login
```

### 2. Нажмите "Забыли пароль?"

### 3. Проверьте что:
- ✅ Правильный стиль (gradient текст "WOXLY")
- ✅ Кнопка "Вернуться к входу" работает
- ✅ Можно ввести email
- ✅ После отправки - 6 полей для кода
- ✅ После кода - создание нового пароля
- ✅ После успеха - кнопка "Войти в аккаунт"

---

## 🔧 Если Что-то Не Работает

### Frontend не обновился:
```bash
# Очистите кеш браузера
Ctrl + F5

# Или пересоберите
cd /root/woxly/apps/frontend
rm -rf dist
npm run build
sudo systemctl reload nginx
```

### Ошибка при сборке:
```bash
cd /root/woxly/apps/frontend
npm install
npm run build
```

### Nginx не работает:
```bash
sudo nginx -t
sudo systemctl status nginx
sudo systemctl restart nginx
```

---

## 📝 Что Изменилось

1. **Стиль** - теперь как на LoginPage
2. **Навигация** - кнопка "Вернуться" работает правильно
3. **API** - правильные эндпоинты `/api/auth/...`
4. **Ввод кода** - 6 отдельных полей с автофокусом
5. **Поток** - Email → Код → Пароль → Успех

---

## 🎉 Готово!

После деплоя восстановление пароля будет работать на:
```
https://woxly.ru/#/auth/forgot-password
```

Время деплоя: ~2 минуты ⏱️
