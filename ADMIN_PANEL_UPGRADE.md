# 🚀 WOXLY Admin Panel - Premium Upgrade

## ✨ Что добавлено:

### 1. **Система бейджей** 
- ✅ `creator` - Создатель (золотой с короной)
- ✅ `admin` - Админ (красный со щитом)
- ✅ `moderator` - Модератор (синий со звездой)
- ✅ `verified` - Верифицирован (зеленый с искрами)
- ✅ `premium` - Premium (фиолетовый с молнией)
- ✅ `early_supporter` - Ранний сторонник (янтарный с наградой)

### 2. **Премиум дизайн**
- Glassmorphism эффекты
- Градиенты и анимации
- Real-time индикаторы
- Современные карточки с hover эффектами
- Адаптивная сетка

### 3. **Расширенная аналитика**
- Топ активных пользователей
- Статистика по бейджам
- Графики роста (по периодам: 24ч, 7д, 30д, все время)
- Real-time обновления каждые 30 секунд

### 4. **Управление бейджами**
- Установка/удаление бейджей через админ-панель
- Кастомные цвета бейджей
- Визуальное отображение бейджей в профилях

## 📦 Установка:

### 1. Применить миграцию базы данных:

```bash
cd c:\woxly
npx prisma migrate deploy
```

### 2. Пересобрать backend:

```bash
cd c:\woxly\apps\backend
npm run build
pm2 restart woxly-backend
```

### 3. Пересобрать frontend:

```bash
cd c:\woxly\apps\frontend
npm run build
```

## 🎯 Как установить бейдж "Создатель" на свой профиль:

### Вариант 1: Через админ-панель
1. Войдите в админ-панель: `https://woxly.ru/admin`
2. Перейдите в раздел "Пользователи"
3. Найдите свой профиль
4. Нажмите кнопку "Установить бейдж"
5. Выберите "Создатель" (creator)
6. Сохраните

### Вариант 2: Через SQL (прямой доступ к БД)

```sql
-- Замените YOUR_USER_ID на ваш ID пользователя
UPDATE users 
SET badge = 'creator', badge_color = '#FFD700' 
WHERE id = YOUR_USER_ID;
```

### Вариант 3: Через API (curl)

```bash
curl -X POST https://woxly.ru/api/admin/users/YOUR_USER_ID/badge \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"badge": "creator", "badgeColor": "#FFD700"}'
```

## 🎨 Доступные бейджи и их цвета:

| Бейдж | Код | Цвет | Описание |
|-------|-----|------|----------|
| 👑 Создатель | `creator` | `#FFD700` | Основатель проекта |
| 🛡️ Админ | `admin` | `#dc143c` | Администратор |
| ⭐ Модератор | `moderator` | `#3b82f6` | Модератор |
| ✨ Верифицирован | `verified` | `#10b981` | Подтвержденный аккаунт |
| ⚡ Premium | `premium` | `#8b5cf6` | Premium пользователь |
| 🏆 Ранний сторонник | `early_supporter` | `#f59e0b` | Ранний участник |

## 🔥 Новые фичи админ-панели:

### Dashboard:
- ✅ Real-time статистика с автообновлением
- ✅ Топ-5 активных пользователей
- ✅ Статистика по бейджам
- ✅ Графики роста по периодам
- ✅ Индикаторы изменений (+/-)

### Управление пользователями:
- ✅ Установка/удаление бейджей
- ✅ Кастомные цвета бейджей
- ✅ Расширенная информация о пользователях
- ✅ Фильтры по статусам и бейджам

### Дизайн:
- ✅ Glassmorphism header
- ✅ Градиентные кнопки и карточки
- ✅ Анимированные иконки
- ✅ Hover эффекты
- ✅ Адаптивная сетка

## 📊 API Endpoints:

### Получить расширенную статистику:
```
GET /api/admin/stats/advanced?period=7d
```

### Установить бейдж пользователю:
```
POST /api/admin/users/:id/badge
Body: {
  "badge": "creator",
  "badgeColor": "#FFD700"
}
```

### Удалить бейдж:
```
POST /api/admin/users/:id/badge
Body: {
  "badge": null,
  "badgeColor": null
}
```

## 🎉 Готово!

Теперь у вас есть премиум админ-панель с системой бейджей!

Чтобы установить себе бейдж "Создатель":
1. Примените миграцию
2. Перезапустите backend
3. Войдите в админ-панель
4. Установите себе бейдж через интерфейс

Бейдж будет отображаться:
- ✅ В профиле пользователя
- ✅ В списке друзей
- ✅ В чатах
- ✅ В админ-панели
- ✅ В топе активных пользователей









