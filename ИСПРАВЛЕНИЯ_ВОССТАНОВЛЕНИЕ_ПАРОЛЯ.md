# ✅ Исправления: Восстановление Пароля

## Что Исправлено

### 1. ✅ Стиль страницы
- Использованы цвета из проекта (gradient-text)
- Убраны лишние Card компоненты
- Применен стиль как на LoginPage

### 2. ✅ Кнопка "Вернуться"
- Исправлен путь: `/auth/login` вместо `/login`
- Теперь корректно возвращает на страницу входа

### 3. ✅ API эндпоинты
- Исправлены пути: `/api/auth/...` вместо `/auth/...`
- Добавлен правильный URL для запросов

### 4. ✅ Заголовок окна
- Изменено с "Woxly" на "WOXLY"
- В `apps/desktop/src/main.ts`

### 5. ✅ Улучшен ввод кода
- 6 отдельных полей для цифр (как на скриншоте)
- Автофокус на следующее поле
- Backspace возвращает на предыдущее

## 📝 Измененные Файлы

1. `apps/frontend/src/pages/auth/ForgotPasswordPage.tsx` - полностью переделан
2. `apps/desktop/src/main.ts` - изменен title на "WOXLY"

## 🚀 Как Протестировать

### 1. Запустите Desktop приложение:
```bash
cd C:\woxly\apps\desktop
npm run dev
```

### 2. Проверьте страницу восстановления:
1. Откройте приложение
2. Нажмите "Забыли пароль?"
3. Введите email
4. Нажмите "Отправить код подтверждения"
5. Введите 6-значный код
6. Создайте новый пароль

### 3. Проверьте заголовок окна:
- Должно быть "WOXLY" вместо "WOXLY Desktop"

## 🎨 Стиль Теперь Соответствует

- ✅ Gradient текст для заголовка
- ✅ Правильные цвета кнопок
- ✅ Правильный фон
- ✅ Правильные отступы
- ✅ Правильные сообщения об ошибках

## 🔄 Поток Восстановления

```
1. Email → 2. Код (6 цифр) → 3. Новый пароль → 4. Успех
```

Каждый шаг имеет:
- Кнопку "Вернуться к входу"
- Правильные сообщения об ошибках
- Правильную валидацию

## 📦 Деплой на Сервер

После тестирования загрузите на сервер:

**Файлы для обновления:**
1. `apps/frontend/src/pages/auth/ForgotPasswordPage.tsx`
2. `apps/desktop/src/main.ts` (только для desktop)

**Команды на сервере:**
```bash
cd /root/woxly
cd apps/frontend
npm run build
sudo systemctl reload nginx
```

## ✅ Готово!

Теперь восстановление пароля работает правильно:
- Правильный стиль ✅
- Правильная навигация ✅
- Правильные API запросы ✅
- Правильный заголовок окна ✅
